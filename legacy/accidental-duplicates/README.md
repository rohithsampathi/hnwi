These files were found as accidental `* 2.ts` and `* 2.tsx` copies inside the live `app/` tree.

They were moved out of the route tree on 2026-04-06 so Next.js would not crawl junk files during startup.

They are preserved here for reference only and are not part of the active application.
